{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "",
  "author": "{{author}}",
  "keywords": [],
  "main": "dist/index.js",
  "bin": {
    "{{name}}": "./bin/index.js"
  },
  "scripts": {
    "build": "NODE_ENV=production npm run clean && tsc && tsc-alias",
    "clean": "rimraf dist",
    "dev": "NODE_ENV=development tsx src/index.ts",
    "start": "NODE_ENV=production node dist/index.js"
  }
}
