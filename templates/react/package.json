{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "",
  "author": "{{author}}",
  "keywords": [],
  "main": "index.js",
  "scripts": {
    "build": "NODE_ENV=production vite build",
    "dev": "NODE_ENV=development vite",
    "preview": "npm run build && vite preview",
    "start": "npm run dev"
  }
}
